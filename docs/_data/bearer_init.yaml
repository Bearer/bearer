name: bearer init
synopsis: Generates a default config to `bearer.yml`
usage: bearer init [flags]
options:
  - name: help
    shorthand: h
    default_value: "false"
    usage: help for init
see_also:
  - "bearer - "
