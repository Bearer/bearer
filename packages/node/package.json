{
  "private": false,
  "name": "@bearer/node",
  "version": "1.6.0",
  "description": "Bearer platform bindings",
  "keywords": [
    "node",
    "express",
    "bearer"
  ],
  "author": "Bearer Team<engineering@bearer.sh>",
  "repository": "Bearer/bearer",
  "homepage": "https://github.com/Bearer/bearer/packages/node#readme",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/"
  ],
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf lib",
    "prepare": "yarn clean && yarn build",
    "prepack": "yarn clean && NODE_ENV=production yarn build",
    "test": "jest --colors",
    "test:ci": "jest --coverage --colors"
  },
  "bugs": {
    "url": "https://github.com/Bearer/bearer/issues"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/nock": "^9.3.1",
    "jest": "^24.7.1",
    "nock": "^10.0.6",
    "ts-jest": "^24.0.1",
    "typescript": "^3.4.1"
  },
  "dependencies": {
    "axios": "^0.18.0"
  }
}
