{
  "name": "@bearer/security",
  "version": "0.100.0",
  "private": false,
  "engines": {
    "node": "<11"
  },
  "description": "Bearer tooling for encrypting and decrypting messages",
  "keywords": [
    "security",
    "bearer"
  ],
  "author": "Bearer Team<engineering@bearer.sh>",
  "repository": "Bearer/bearer",
  "homepage": "https://github.com/Bearer/bearer/packages/security#readme",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "directories": {
    "lib": "lib",
    "test": "__test__"
  },
  "files": [
    "lib/"
  ],
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "@types/node": "^10.12.17",
    "jest": "^24.1.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3333"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "start": "tsc --watch",
    "build": "tsc -p tsconfig.build.json",
    "prepare": "yarn build",
    "prepack": "yarn clean && yarn build",
    "test": "jest --colors",
    "test:ci": "jest --coverage --colors"
  }
}
