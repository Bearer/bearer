{
  "private": false,
  "name": "@bearer/express",
  "version": "1.6.1",
  "description": "Express webhook middleware",
  "keywords": [
    "node",
    "express",
    "bearer"
  ],
  "author": "Bearer Team<engineering@bearer.sh>",
  "repository": "Bearer/bearer",
  "homepage": "https://github.com/Bearer/bearer/packages/express#readme",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/"
  ],
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf lib",
    "prepare": "yarn clean && yarn build",
    "prepack": "yarn clean && NODE_ENV=production yarn build",
    "test": "jest --colors",
    "test:ci": "jest --coverage --colors"
  },
  "bugs": {
    "url": "https://github.com/Bearer/bearer/issues"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/jest": "^24.0.11",
    "@types/on-headers": "^1.0.0",
    "@types/supertest": "^2.0.7",
    "jest": "^24.8.0",
    "supertest": "^3.4.2",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "@bearer/logger": "^1.6.1",
    "@bearer/security": "^1.6.1",
    "express": "^4.17.1",
    "on-headers": "^1.0.1"
  }
}
