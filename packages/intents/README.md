# @bearer/intents

## What

Set of tools facilitating Bearer Intents writing.

## Why

Bearer Intents are lambda running in the cloud. Intents package helps scenario builders to focus on data fetching/saving instead of lambda complexity.

We have defined multiple Intent types so that scenario builders take adavantages of them.

Example of pre-built abstractions:

- data saving/fetching
- lambda callbacks
