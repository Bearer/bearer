{
  "private": false,
  "name": "@bearer/types",
  "version": "1.6.1",
  "description": "Provide typing for Bearer packages",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": "Bearer/bearer",
  "homepage": "https://github.com/Bearer/bearer/packages/types#readme",
  "author": "Bearer Team<engineering@bearer.sh>",
  "files": [
    "lib/"
  ],
  "scripts": {
    "clean": "rm -rf lib/",
    "build": "tsc",
    "start": "yarn build -w",
    "test": "jest --colors",
    "test:ci": "jest --coverage --runInBand --colors",
    "prepare": "yarn clean && yarn build",
    "prepack": "yarn clean && NODE_ENV=production yarn build"
  },
  "keywords": [
    "types",
    "typescript",
    "bearer"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  },
  "jest": {
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "testRegex": "(/__tests__/.*|(\\.|/)(test|spec))\\.(jsx?|tsx?)$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js",
      "jsx",
      "json",
      "node"
    ]
  }
}
