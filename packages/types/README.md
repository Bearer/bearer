# @bearer/types

## What

Typescript's common Bearer types defintions.

## Why

Ensure common data structure are used across packages so that everyo data structure changes leading to error are automatically catch at build time
