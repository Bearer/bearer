{
  "name": "@bearer/x-ray",
  "version": "0.100.0",
  "description": "Trace outgoing traffic inside the intent",
  "main": "lib/index.js",
  "license": "MIT",
  "dependencies": {
    "@bearer/logger": "^0.95.1",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/debug": "^4.1.2",
    "@types/jest": "^24.0.9",
    "@types/uuid": "^3.4.4",
    "@types/node": "^11.10.5",
    "aws-sdk": "^2.415.0",
    "jest": "^24.3.1",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.3333"
  },
  "files": [
    "/lib"
  ],
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf lib",
    "prepack": "yarn clean && yarn build",
    "prepare": "yarn clean && yarn build",
    "test": "jest --colors",
    "test:ci": "jest --coverage --colors"
  }
}
