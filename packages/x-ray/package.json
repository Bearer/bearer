{
  "name": "@bearer/x-ray",
  "version": "0.109.0",
  "description": "Trace outgoing traffic inside the intent",
  "main": "lib/index.js",
  "license": "MIT",
  "dependencies": {
    "@bearer/logger": "^0.109.0",
    "uuid": "^3.3.2"
  },
  "devDependencies": {
    "@types/debug": "^4.1.3",
    "@types/jest": "^24.0.11",
    "@types/node": "^11.10.5",
    "@types/uuid": "^3.4.4",
    "aws-sdk": "^2.415.0",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.1",
    "typescript": "^3.4.1"
  },
  "files": [
    "/lib"
  ],
  "scripts": {
    "start": "tsc --watch",
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf lib",
    "prepack": "yarn clean && yarn build",
    "prepare": "yarn clean && yarn build",
    "test": "jest --colors",
    "test:ci": "jest --coverage --colors"
  }
}
