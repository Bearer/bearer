{
  "name": "{{componentTagName}}",
  "version": "0.0.1",
  "description": "{{componentName}}",
  "main": "dist/index.js",
  "types": "dist/types/components.d.ts",
  "module": "dist/esm/index.js",
  "collection": "dist/collection/collection-manifest.json",
  "scripts": {
    "bearer": "bearer",
    "push": "bearer push",
    "start": "bearer start",
    "invoke": "bearer invoke",
    "generate": "bearer generate"
  },
  "dependencies": {
    "@bearer/functions": "{{bearerTagVersion}}",
    {{bearerRestClient}}
  },
  "devDependencies": {
    "@bearer/cli": "{{bearerTagVersion}}",
    "@types/node": "^10.12.25"
  },
  "license": "MIT"
}
