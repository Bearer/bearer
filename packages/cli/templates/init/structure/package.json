{
  "name": "{{componentTagName}}",
  "version": "0.0.1",
  "description": "{{componentName}}",
  "main": "dist/index.js",
  "types": "dist/types/components.d.ts",
  "module": "dist/esm/index.js",
  "collection": "dist/collection/collection-manifest.json",
  "scripts": {
    "push": "bearer push",
    "start": "bearer start",
    "invoke": "bearer invoke",
    "generate": "bearer generate"
  },
  "dependencies": {
    "@bearer/core": "{{bearerTagVersion}}",
    "@bearer/intents": "{{bearerTagVersion}}",
    "@bearer/ui": "{{bearerTagVersion}}",
    "axios": "^0.18.0"
  },
  "devDependencies": {
    "@bearer/cli": "{{bearerTagVersion}}",
    "@bearer/js": "{{bearerTagVersion}}",
    "@types/node": "^10.12.25"
  },
  "peerDependencies": {
    "@bearer/js": "{{bearerTagVersion}}"
  },
  "license": "MIT"
}
