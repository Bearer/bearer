# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: arbornetworks-docker-public-docker.bintray.io/circleci-node-aws:8.4.0

version: 2
jobs:
  build:
    <<: *defaults
    steps:
      - checkout
      - run:
          name: Authenticate through NPM registry
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > ~/repo/.npmrc
      # # Download and cache dependencies
      # - restore_cache:
      #    keys:
      #    - v1-dependencies-{{ checksum "package.json" }}
      #    # fallback to using the latest cache if no exact match is found
      #    - v1-dependencies-
      - run: yarn install --frozen-lockfile
      - run: yarn run lerna bootstrap --ci
      # - save_cache:
      #    paths:
      #      - node_modules
      #    key: v1-dependencies-{{ checksum "package.json" }}

      # run tests!
      - run:
          name: Run tests
          command: yarn test
          environment:
            NODE_ENV: test
      - persist_to_workspace:
          root: ~/repo
          paths: .

  deploy_storybook:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - run:
          name: Authenticate through NPM registry
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > ~/repo/.npmrc
      - run:
          name: Build Storybook
          command: yarn run lerna run build-storybook --scope=@bearer/ui
      - run:
          name: Prepare deploy
          command: aws configure set default.s3.signature_version s3v4
      - run:
          name: Deploy
          command: cd packages/ui/storybook-static && aws s3 sync . s3://ui.docs.bearer.sh/ --delete --region eu-west-3

workflows:
  version: 2
  btd:
    jobs:
      - build
      - deploy_storybook:
          requires:
            - build
          filters:
            branches:
              only: master
