type: "risk"
patterns:
  - |
    logger.info(<$ARGUMENT>)
  - |
    Rails.logger.info(<$ARGUMENT>)
languages:
  - ruby
detect_rails_session:
type: "risk"
patterns:
  - |
    session[...] = $ANYTHING
languages:
  - ruby
