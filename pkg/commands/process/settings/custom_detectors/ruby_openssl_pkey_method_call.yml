type: "risk"
patterns:
  - pattern: |
      $<_>.$<METHOD>($<DATA_TYPE>)
    filters:
      - variable: METHOD
        values:
          - export
          - to_pem
          - to_s
      - variable: DATA_TYPE
        detection: datatype
languages:
  - "ruby"
