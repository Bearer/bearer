encryption_detections:
  message: "encryption fails detected"
  level: "warning"
  query: |
    risks = data.bearer.encryption_fails.risks
  modules:
    - path: processors/encryption_fails.rego
      name: bearer.encryption_fails
# ignore tanker encrypted
# validate that there is a className for objectnaame else just output it without validation
