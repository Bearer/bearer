high:
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 1
      filename: unsafe_params.rb
      parent_line_number: 1
      snippet: RubyVM::InstructionSequence.compile(params["oops"])
      fingerprint: d047dec078244cc582c4250294366633_0
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 3
      filename: unsafe_params.rb
      parent_line_number: 3
      snippet: a.eval(params["oops"], "test")
      fingerprint: d047dec078244cc582c4250294366633_1
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 5
      filename: unsafe_params.rb
      parent_line_number: 5
      snippet: a.instance_eval(params["oops"])
      fingerprint: d047dec078244cc582c4250294366633_2
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 7
      filename: unsafe_params.rb
      parent_line_number: 7
      snippet: a.class_eval(params["oops"])
      fingerprint: d047dec078244cc582c4250294366633_3
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 9
      filename: unsafe_params.rb
      parent_line_number: 9
      snippet: a.module_eval(params["oops"])
      fingerprint: d047dec078244cc582c4250294366633_4
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 11
      filename: unsafe_params.rb
      parent_line_number: 11
      snippet: eval(params["oops"])
      fingerprint: d047dec078244cc582c4250294366633_5
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 13
      filename: unsafe_params.rb
      parent_line_number: 13
      snippet: instance_eval(params["oops"], "test")
      fingerprint: d047dec078244cc582c4250294366633_6
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 15
      filename: unsafe_params.rb
      parent_line_number: 15
      snippet: class_eval(params["oops"])
      fingerprint: d047dec078244cc582c4250294366633_7
    - rule:
        cwe_ids:
            - "94"
            - "95"
        id: ruby_lang_eval_using_user_input
        title: Potential command injection with user input detected.
        description: |
            ## Description
            It is dangerous to use eval with user input, or to compile code with user-supplied data. Such practices can lead to command injection.

            ## Remediations
            ❌ Avoid using code execution methods with unsanitized user input.

            It might be possible to use dynamic hardcoded values, instead of user input directly.

            ```ruby
            get_total_str = if params["include_vat"]
              "def dynamic(a,b,c); a + b + c; end"
            else
              "def dynamic(a,b); a + b; end"
            end

            get_total = eval(get_total_str)
            ```

            ## Resources
            - [OWASP Code injection explained](https://owasp.org/www-community/attacks/Code_Injection)
        documentation_url: https://docs.bearer.com/reference/rules/ruby_lang_eval_using_user_input
      line_number: 17
      filename: unsafe_params.rb
      parent_line_number: 17
      snippet: module_eval(params["oops"])
      fingerprint: d047dec078244cc582c4250294366633_8

