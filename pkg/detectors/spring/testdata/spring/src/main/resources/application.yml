logging:
  file:
    name: myapplication.log
bael:
  property: defaultValue
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    password: password
    url: jdbc:h2:dev
    username: SA
