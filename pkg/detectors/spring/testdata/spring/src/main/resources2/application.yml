logging:
  file:
    name: myapplication.log
bael:
  property: defaultValue
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL}
    username: SA
    password: password
    driver-class-name: com.mysql.jdbc.Driver
