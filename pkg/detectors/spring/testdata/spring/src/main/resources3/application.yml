logging:
  file:
    name: myapplication.log
bael:
  property: defaultValue
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL}
    username: SA
    password: password
    driverClassName: com.postgresql.jdbc.Driver
